spring:
  application:
    name: microservice4
  cloud:
    config:
      uri: http://configserver:8888
  security:
    user:
      name: root
      password: password
    
server:
  port: 8093
  
management:
  endpoints:
    web:
      exposure:
        include: "*"