spring:
  application: 
    name: EurekaServer2
  cloud:
     config:
        uri: http://configserver:8888
server:
    port: 8762

management:
  endpoints:
    web:
      exposure:
        include: "*"