spring:
  application: 
    name: EurekaServer1
  cloud:
     config:
        uri: http://configserver:8888
server:
    port: 8761

management:
  endpoints:
    web:
      exposure:
        include: "*"